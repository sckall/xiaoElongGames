# Agent 持久记忆（用户要求）

1. **截图留档**：任何截图/录屏都必须保存到工作区内 `docs/screenshots/` 留档，
   文件名带日期与用途；截图前确认浏览器无敏感信息。
2. **跨工作区/全局操作**：任何工作区外的全局操作（依赖安装、端口监听、系统
   目录、远程服务器、外网 API、进程管理等）都要先谨慎评估并记录到
   `tools/RISK_LOG.md`，再执行；禁止危险操作（sudo、系统设置修改等）。
